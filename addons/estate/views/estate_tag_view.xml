<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <!-- Add field to tree view -->
        <record id="view_estate_tag_tree" model="ir.ui.view">
            <field name="name">estate.property.tag.tree</field>
            <field name="model">estate.property.tag</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>
                </tree>
            </field>
        </record>

         <!-- Add field to form view -->
        <record id="view_estate_tag_form" model="ir.ui.view">
            <field name="name">estate.property.tag.form</field>
            <field name="model">estate.property.tag</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <!-- Add other fields -->
                        <field name="name"/>
                    </sheet>
                </form>
            </field>
        </record>
        
        <!-- Add field to search view -->
        <record id="view_estate_tag_search" model="ir.ui.view">
            <field name="name">estate.property.tag.search</field>
            <field name="model">estate.property.tag</field>
            <field name="arch" type="xml">
                <search>
                    <field name="name" string="Name"/>
                    <!-- Add other fields -->
                </search>
            </field>
        </record>

</odoo>
